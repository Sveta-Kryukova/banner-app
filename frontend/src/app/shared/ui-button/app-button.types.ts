import type { ThemePalette } from "@angular/material/core";

/** Maps to Material `matButton` appearance: text / filled / outlined. */
export type AppButtonVariant = "text" | "flat" | "stroked";

export type AppButtonColor = ThemePalette;
