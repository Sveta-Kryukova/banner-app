/**
 * Replace `apiBaseUrl` with your deployed API origin before `ng build`
 * (e.g. https://my-api.onrender.com/api). Same origin as the Angular app is OK if you proxy /api.
 */
export const environment = {
  production: true,
  apiBaseUrl: "https://YOUR-BACKEND-HOST/api",
};
